checkpoint_file: './models/wavenet_aw_DC_SDSDR_SNR_Loss.ckpt'
model: 'wavenet'

export_filename: './models/onnx/aw_wavenet.onnx'
sample_block_size: 1024 # check dataset blocksize during training, it should be matching