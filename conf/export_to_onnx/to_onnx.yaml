checkpoint_file: './models/waveunet_aug_mrLoss.ckpt'
model: 'waveunet'

export_filename: './models/onnx/waveunet_aug_mrLoss.onnx'
sample_block_size: 1024 # check dataset blocksize during training, it should be matching